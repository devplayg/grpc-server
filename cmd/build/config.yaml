app:
  receiver:
    address:
    classifier:
      address:

  classifier:
    address:
    notifier:
      address:

  notifier:
    address:

  calculator:
    interval: 3000

  storage:
    address: 127.0.0.1:9001
    access-key: devplayg
    secret-key: devplayg12!@
    bucket: vas

spring:
  profiles: dev
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/devplayg?useUnicode=yes&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: Uniiot12!@